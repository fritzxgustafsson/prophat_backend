services:
  iris:
    image: prophat-db:latest
    container_name: iris
    hostname: iris
    volumes:
      - /opt/prophat/persistent/db:/persistent
    ports:
      - "52773:52773"
  
  webgateway:
    image: prophat-web:latest
    container_name: webgateway
    hostname: webgateway
    volumes:
      - /opt/prophat/persistent/web:/persistent
    ports:
      - "80:80"